<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Welcome to PhotoSite</title>
</head>
<body>
<div>
  <h2>List of PhotoSite</h2>
  <h3>User's Photo Page</h3>
</div>
<div>
  <h2>Photos of <%= link_to @user.first_name %> <%= @user.last_name %></h2>
  <% @photos.each do |photo| %>
    <%= image_tag("/assets/#{photo.file_name}") %>
    <p> Photo posted at   <%= photo.date_time %></p>

    <% @comments.where("photo_id=?",photo.id).each do |comment| %>

      <p> Comment: <%= comment.comment_text %></p>
      <p> Created at: <%= comment.date_time %></p>
      <p> Posted by: <%= link_to display_name(comment.user_id) , user_photos_path(comment.user_id) %></p>

    <% end %>
    </div>
  <%end%>
  </body>
</html>
